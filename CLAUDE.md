# CLAUDE.md

## Package Manager

Use **bun** for all package management and script execution. Never use npm or yarn.

- Install: `bun add <package>`
- Dev install: `bun add -d <package>`
- Run scripts: `bun run <script>`
